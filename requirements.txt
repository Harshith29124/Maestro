# Runtime dependencies (installed on Vercel/Railway).
fastapi==0.115.6
httpx==0.28.1
pydantic==2.10.4
PyYAML==6.0.2
python-dotenv==1.0.1

# ASGI server for local dev and Railway/containers. Vercel uses its own runtime;
# this is harmless there. Pinned without [standard] extras to keep cold starts lean.
uvicorn==0.34.0
